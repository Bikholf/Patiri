export type Group = {
    id: string;
    name: string,
    slug: string,
    description: string,
    maxMembers: number,
    createdBy: string,
    createdAt: string,
    updatedAt: string,
    actions: any
};